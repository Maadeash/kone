# DriveSentinel v5 + v2 multi-stage
#
# torch: install the CUDA build to match your driver, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# The CPU build works for everything except training speed:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# This machine has no NVIDIA GPU (Intel UHD only), so the CPU build is installed.

# --- core (v5 bearing pipeline, frozen) ---
numpy>=1.24
scipy>=1.11
pandas>=2.0
pyarrow>=14.0
joblib>=1.3
torch>=2.0
pytest>=7.0

# --- v2 multi-stage additions ---
nptdms>=1.9          # D2 KAIST .tdms reader
scikit-learn>=1.3    # gradient-boosting + metrics baselines for B-S4 / B-S1
matplotlib>=3.7      # figures for results and data notes
streamlit>=1.30      # P5 dashboard
pypdf>=4.0           # read Sensor_raw_data_conversion_formulas.pdf (D3, see workflow §13.5)
