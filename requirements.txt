# DriveSentinel v5
#
# torch: install the CUDA build to match your driver, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# The CPU build works for everything except training speed.
numpy>=1.24
scipy>=1.11
pandas>=2.0
pyarrow>=14.0
joblib>=1.3
torch>=2.0
pytest>=7.0
